import { JanixShopValidators } from './janix-shop-validators';

describe('JanixShopValidators', () => {
  it('should create an instance', () => {
    expect(new JanixShopValidators()).toBeTruthy();
  });
});
